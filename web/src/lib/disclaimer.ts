// Mirrored from api/src/modules/reports/disclaimer.ts. The backend enforces it
// in every exported report; the UI repeats it on every results view so the
// caveat is visible regardless of whether the user exports.

export const DISCLAIMER =
  'This report provides automated availability intelligence only. It is not legal advice, does not guarantee registrability, and should not replace professional trademark or legal review.';
