{
  "name": "nameforge",
  "version": "0.1.0",
  "private": true,
  "description": "NameForge — name availability intelligence platform (Loman Cavendish)",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev": "docker compose up",
    "dev:down": "docker compose down"
  }
}
